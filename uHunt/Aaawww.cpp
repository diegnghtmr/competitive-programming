#include <array>
#include <iostream>;
#include <vector>
#include <string>
#include <ostream>;
using namespace std;

int main() {
    int n,m,r;
    string GANANDO = "Ooohhh... Aaayyy!!!";
    string PERDIENDO = "Ooohhh... Aaawww...";

    while (cin>>n>>m>>r) {
        vector<string> estados(n-1);
        for (int i = 0; i<estados.size(); i++) {
            string cadena;
            cin>>cadena;
            estados[i] = cadena;
        
        }

        for (:; condition; inc-expression) {
        
        }
    }

    return 0;
}
